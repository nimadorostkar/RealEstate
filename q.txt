

<!-- The Modal -->
<div id="myModal{{ Order_incomings.id }}" class="modal">
  <!-- Modal content -->
  <div class="modal-content" align="right">
    <span class="close close{{ Order_incomings.id }}">&times;</span>

    <form action="{% url 'incomings_edit' %}" method="post">
       {% csrf_token %}

        <div class="row">
          <div class="col-md-12">
            <div class="form-group">
              <label><p class="text-xs text-secondary mb-0"> توضیحات </p></label>
              <textarea name="description" class="form-control" rows="2" cols="50"> {{ Order_incomings.description }} </textarea>
            </div>
          </div>
          <div class="col-lg-12">
            <div class="form-group">
              {% for field in timeform %} {{ field }} {{ value }} {% endfor %}
            </div>
            <p class="text-xxs text-secondary" dir="rtl"> فرمت صحیح تاریخ: 00-00-1400 و زمان: 00:00  </p>
          </div>
        </div>
        <input name="reqId" type="hidden" value="{{ req.id }}">
        <input name="incomId" type="hidden" value="{{ Order_incomings.id }}">
        <div align="center">
          <span class="text-xs text-secondary mb-0"> حذف </span> &nbsp; <input name="remove" type="checkbox">
          <br><br>
          <button type="submit" class="btn bg-gradient-info my-1 me-1"> ثبت </button>
        </div>

      </form>

  </div>
</div>






<style>
    /* The Modal (background) */
    .modal {
      display: none; /* Hidden by default */
      position: fixed; /* Stay in place */
      padding-top: 100px; /* Location of the box */
      left: 0;
      top: 0;
      width: 100%; /* Full width */
      height: 100%; /* Full height */
      overflow: auto; /* Enable scroll if needed */
      background-color: rgb(0,0,0); /* Fallback color */
      background-color: rgba(0,0,0,0.4); /* Black w/ opacity */
    }

    /* Modal Content */
    .modal-content {
      background-color: #fefefe;
      margin: auto;
      padding: 20px;
      border: 1px solid #888;
      width: 90%;
    }

    /* The Close Button */
    .close {
      color: #aaaaaa;
      float: right;
      font-size: 28px;
      font-weight: bold;
    }

    .close:hover,
    .close:focus {
      color: #000;
      text-decoration: none;
      cursor: pointer;
    }

</style>







<script>
 {% for Order_incomings in incomings %}

    // Get the modal
    var modal{{ Order_incomings.id }} = document.getElementById("myModal"+{{ Order_incomings.id }});

    // Get the button that opens the modal
    var btn{{ Order_incomings.id }} = document.getElementById("myBtn"+{{ Order_incomings.id }});

    // Get the <span> element that closes the modal
    var span{{ Order_incomings.id }} = document.getElementsByClassName("close"+{{ Order_incomings.id }})[0];

    // When the user clicks the button, open the modal
    btn{{ Order_incomings.id }}.onclick = function() {
    modal{{ Order_incomings.id }}.style.display = "block";
    }

    // When the user clicks on <span> (x), close the modal
    span{{ Order_incomings.id }}.onclick = function() {
    modal{{ Order_incomings.id }}.style.display = "none";
    }

    // When the user clicks anywhere outside of the modal, close it
    window.onclick = function(event) {
    if (event.target == modal{{ Order_incomings.id }}) {
      modal{{ Order_incomings.id }}.style.display = "none";
    }
    }

 {% endfor %}
</script>






---------------------





<form action="{% url 'addarea' %}" method="post">
   {% csrf_token %}
   <input name="reqId" type="hidden" value="{{ req.id }}">
   <input name="incomId" type="hidden" value="{{ Order_incomings.id }}">
   <button type="submit" style="width:50px;height:9px;border: 0 none transparent;padding:0;margin:0;font-size:7px;">+ افزودن جدید</button>
</form>
